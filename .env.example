# CropIntel Website environment example
#
# This static website does not require secrets to build or run.
# Keep real values in local or deployment secret stores. Do not commit them.

# Optional future contact form settings. These are not used by the current app.
RESEND_API_KEY=
CONTACT_EMAIL=support@cropintel.app

# Required only for CI/deploy, stored as GitHub Actions secrets.
# FIREBASE_SERVICE_ACCOUNT_CROPINTEL_HOME={"type":"service_account",...}
